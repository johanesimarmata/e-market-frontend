import React from 'react'
import {ApplicationRoutes} from './ApplicationRoutes'
export const Layout = () => {
     return(
          <>
               {/**Navbar**/}
               <ApplicationRoutes/>
               {/**Footer (if needed)**/}
          </>
     )
}